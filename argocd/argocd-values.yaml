# ArgoCD configuration
server:
  service:
    type: NodePort
    ports:
      http: 80
configs:
  params:
    server.insecure: "true"  
controller:
  resources:
    requests:
      cpu: 250m
      memory: 256Mi